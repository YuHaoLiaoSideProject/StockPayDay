name = "kvdb-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
KVDB_BASE = "https://kvdb.io"

# Secrets must be set via `wrangler secret put`:
# SECRET_KEY  — kvdb.io 管理金鑰
# SIGNING_KEY — 用來簽 access token 的金鑰
