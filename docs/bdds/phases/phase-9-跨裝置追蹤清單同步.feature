# language: zh-TW
@phase-9 @frontend
Feature: 跨裝置追蹤清單同步
  作為一個投資人
  我希望在配對的裝置之間自動同步追蹤清單
  以便在任何裝置上都能看到一致的追蹤清單（免登入、保持純靜態站）

  # 對應 Interaction Flow：步驟 1-4（啟用同步）、5（狀態顯示）、6-7（跨裝置增刪）、
  # 8（立即同步）、9（離線恢復）、10（429 退避）、11-12（停用/重新啟用）、
  # 13-14（匯出/匯入備援）、§5 異常處理表格

  Background:
    Given 使用者已進入追蹤清單頁（/watchlist）
    And 追蹤清單以本機瀏覽器為主要儲存（offline-first）
    And 跨裝置同步為選配功能

  # ==================== 啟用同步（步驟 1-4、12） ====================

  @smoke
  Scenario: 貼上配對碼啟用同步
    Given 使用者尚未貼上配對碼
    When 使用者貼上有效的配對碼並點擊「啟動」
    Then 同步狀態列顯示「同步中…」
    And 配對碼記住於本機
    And 重新整理頁面後仍維持已配對狀態

  @edge-case
  Scenario: 配對碼輸入框為空時無法啟動
    Given 使用者尚未貼上配對碼
    When 使用者未輸入配對碼直接點擊「啟動」
    Then 系統不送出啟動請求
    And 同步維持未啟用狀態

  @smoke
  Scenario: 首次配對（雲端尚無清單）上傳建立雲端清單
    Given 使用者的裝置已啟用同步（已貼上有效配對碼）
    And 該配對碼對應的雲端尚無追蹤清單
    When 首次同步執行
    Then 本機追蹤清單上傳並建立雲端清單
    And 同步狀態列顯示「已同步」及上次同步時間
    And 本機清單內容不變

  Scenario: 首次配對（雲端已有清單）合併為並集
    Given 使用者的裝置已啟用同步（已貼上有效配對碼）
    And 雲端已存在另一台裝置的追蹤清單
    When 首次同步執行
    Then 雲端清單與本機清單合併呈現為並集
    And 本機清單包含另一台裝置追蹤的股票
    And 同步狀態列顯示「已同步」

  # ==================== 未配對裝置維持現況（步驟 1） ====================

  @smoke
  Scenario: 未配對裝置既有操作行為與現況完全一致
    Given 使用者尚未貼上配對碼
    When 使用者執行既有追蹤操作（搜尋、加入、移除、切換顯示模式）
    Then 所有既有操作行為與先前版本完全一致
    And 系統不發送任何同步請求

  @edge-case
  Scenario: 未配對時顯示同步設定區塊
    Given 使用者尚未貼上配對碼
    When 追蹤清單頁載入完成
    Then 顯示「🔄 跨裝置同步（選配）」區塊與配對碼輸入框
    And 區塊說明「不設定則完全不影響現有功能」

  # ==================== 同步狀態顯示（步驟 5） ====================

  Scenario Outline: 同步狀態列顯示目前同步狀態
    Given 使用者的裝置已啟用同步
    And 同步處於<狀態情形>
    When 使用者檢視追蹤清單頁頂部的同步狀態列
    Then 狀態列顯示<狀態文字>

    Examples:
      | 狀態情形   | 狀態文字                                 |
      | 同步進行中 | 「同步中…」                               |
      | 同步完成   | 「已同步」並附上次同步時間               |
      | 同步失敗   | 「同步失敗」並附錯誤訊息                 |

  # ==================== 跨裝置增刪同步（步驟 6-7） ====================

  @smoke
  Scenario: 已配對裝置新增追蹤後自動寫回雲端
    Given 使用者的裝置已啟用同步
    And 同步狀態為「已同步」
    When 使用者加入追蹤股票 X
    Then 本機清單立即更新（❤️ 圖示變色、徽章 +1）
    And 約 1.5 秒後變更自動寫回雲端
    And 同步狀態短暫顯示「同步中…」後回到「已同步」

  @smoke
  Scenario: 另一台裝置切回頁面即收到新增變更
    Given 裝置 A 與裝置 B 均已啟用同一組配對碼的同步
    And 裝置 A 已於同步狀態下加入追蹤股票 X
    And 裝置 B 的追蹤清單為舊內容（不含股票 X）
    When 使用者將裝置 B 切回追蹤清單頁
    Then 裝置 B 自動拉取雲端清單並與本機合併
    And 股票 X 加入裝置 B 的追蹤清單
    And 兩台裝置的追蹤清單內容一致

  Scenario: 前台每 60 秒自動檢查收到變更
    Given 裝置 A 與裝置 B 均已啟用同一組配對碼的同步
    And 裝置 A 已於同步狀態下加入追蹤股票 X
    And 裝置 B 停留於追蹤清單頁前景超過 60 秒
    When 裝置 B 的前台自動檢查觸發
    Then 自動拉取雲端清單並與本機合併
    And 股票 X 加入裝置 B 的追蹤清單

  Scenario: 移除追蹤以墓碑傳播至另一台裝置
    Given 裝置 A 與裝置 B 均已啟用同一組配對碼的同步
    And 兩台裝置的追蹤清單皆包含股票 X
    When 使用者在裝置 A 移除追蹤股票 X
    Then 裝置 A 的追蹤清單立即移除股票 X
    And 移除的墓碑標記寫回雲端
    When 使用者將裝置 B 切回追蹤清單頁
    Then 股票 X 從裝置 B 的追蹤清單消失

  @business-rules
  Scenario: 同一支股票雙端變更以最後寫入者勝出
    Given 裝置 A 與裝置 B 均已啟用同一組配對碼的同步
    And 裝置 B 的雲端清單含有股票 X 的移除墓碑（較早寫入）
    When 裝置 A 較晚加入追蹤股票 X（較新寫入）
    And 兩台裝置完成同步合併
    Then 股票 X 保留於兩台裝置的追蹤清單（以較新的寫入者勝出）

  # ==================== 立即同步（步驟 8） ====================

  Scenario: 立即同步
    Given 使用者的裝置已啟用同步
    And 同步狀態為「已同步」
    When 使用者點擊「立即同步」
    Then 立即執行一次同步
    And 同步狀態依序顯示「同步中…」→「已同步」

  # ==================== 離線操作與恢復（步驟 9） ====================

  Scenario: 離線時增刪追蹤正常且本地清單不受影響
    Given 使用者的裝置已啟用同步
    And 裝置處於離線狀態（雲端無法連線）
    When 使用者加入追蹤股票 X
    Then 本機追蹤清單正常更新
    And 同步狀態列顯示「同步失敗」及錯誤訊息
    And 本地追蹤清單完整保留

  Scenario: 恢復連線後自動合併離線期間的變更
    Given 使用者的裝置已啟用同步
    And 離線期間使用者已加入追蹤股票 X（尚未寫回雲端）
    When 裝置恢復連線並切回追蹤清單頁
    Then 自動執行同步並合併離線期間的變更
    And 股票 X 保留且與雲端一致
    And 同步狀態列回到「已同步」
    And 不需重新貼上配對碼

  # ==================== 429 速率限制退避（步驟 10） ====================

  @error-handling
  Scenario: 429 速率限制顯示退避訊息並自動恢復
    Given 使用者的裝置已啟用同步
    And 雲端回傳 429 速率限制
    When 同步嘗試執行
    Then 同步狀態列顯示「速率限制（429），N 秒後重試」
    And 退避期間本機追蹤操作完全正常
    And 退避結束後自動重試單次
    And 重試成功後同步狀態恢復「已同步」

  Scenario Outline: 429 退避秒數依 30→60→120 秒遞增
    Given 使用者的裝置已啟用同步
    And 連續第<次數>次遭遇 429 速率限制
    When 同步嘗試執行
    Then 同步狀態列顯示「速率限制（429），<退避秒數>秒後重試」

    Examples:
      | 次數 | 退避秒數 |
      | 1    | 30       |
      | 2    | 60       |
      | 3    | 120      |

  @business-rules
  Scenario: 自動同步僅於頁面可見時執行
    Given 使用者的裝置已啟用同步
    When 追蹤清單分頁切換至背景
    Then 系統不發送自動輪詢請求
    When 追蹤清單分頁回到前景
    Then 立即觸發一次同步檢查
    And 前景期間每 60 秒自動檢查一次

  # ==================== 停用與重新啟用（步驟 11-12） ====================

  Scenario: 停用同步後本地清單保留
    Given 使用者的裝置已啟用同步
    When 使用者點擊「停用」並確認停用
    Then 同步狀態列顯示「未啟用同步」
    And 系統停止自動同步（不再發送同步請求）
    And 本地追蹤清單完整保留
    And 裝置行為回到與未配對時相同

  Scenario: 停用後重新貼配對碼重新啟用同步
    Given 使用者的裝置已停用同步且本地追蹤清單保留
    And 雲端已存在追蹤清單
    When 使用者再次貼上配對碼並點擊「啟動」
    Then 同步狀態列顯示「同步中…」
    And 與雲端既有清單完成合併
    And 裝置重新納入跨裝置同步

  # ==================== 匯出／匯入備援（步驟 13-14） ====================

  Scenario: 匯出追蹤清單取得可攜帶內容
    Given 使用者的追蹤清單已含股票 X（含 ETF 或特別股）
    When 使用者展開「匯出/匯入」區塊並點擊「匯出追蹤清單」
    Then 系統產生匯出文字或分享連結供複製或下載
    And 匯出內容為目前追蹤中的股票（不含已移除項目）

  Scenario: 匯入追蹤清單與本地清單合併且不重複
    Given 使用者在另一台裝置取得匯出文字（含股票 X 與股票 Y）
    And 本地追蹤清單已含股票 X
    When 使用者貼上匯出文字並點擊「匯入」
    Then 匯入內容與目前清單合併
    And 股票 X 維持一筆（重複股票不重複加入）
    And 股票 Y 加入本地追蹤清單

  @error-handling
  Scenario: 匯入格式錯誤時顯示錯誤且本地清單不變
    Given 本地追蹤清單已含股票 X
    When 使用者貼上格式錯誤的內容並點擊「匯入」
    Then 顯示錯誤提示
    And 本地追蹤清單不變

  # ==================== 異常處理（§5 異常處理表格） ====================

  @error-handling
  Scenario: 配對碼無效時同步失敗且本地清單不受影響
    Given 使用者尚未貼上配對碼
    When 使用者貼上無效的配對碼並點擊「啟動」
    Then 同步狀態列顯示「同步失敗」及錯誤訊息
    And 本地追蹤清單不受影響
    And 使用者重新貼上正確的配對碼後可恢復同步

  @error-handling
  Scenario: 配對碼過期（TTL 90 天）時同步持續失敗
    Given 使用者的裝置已啟用同步
    And 配對碼已過期
    When 同步嘗試執行
    Then 同步持續失敗並顯示錯誤訊息
    And 本地追蹤清單不受影響
    And 向擁有者重新開通並貼上新配對碼後恢復同步

  @edge-case
  Scenario: localStorage 不可用時視同未配對
    Given 瀏覽器無法使用 localStorage（如隱私模式）
    When 使用者貼上配對碼並點擊「啟動」
    Then 配對碼無法記住於本機
    And 裝置視同未啟用同步（同步引擎不啟動）
    And 追蹤清單既有操作行為維持正常

  @edge-case
  Scenario: 舊版追蹤資料可正常載入並沿用
    Given 使用者的瀏覽器已存有舊版追蹤資料（無同步欄位）
    When 使用者開啟追蹤清單頁
    Then 舊版追蹤資料正常載入並顯示
    And 未開啟同步時既有操作行為與先前完全一致
    And 啟用同步後舊資料可正常參與合併